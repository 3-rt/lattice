# OpenClaw gateway auth token (from gateway config — authenticates the WebSocket connection)
OPENCLAW_GATEWAY_TOKEN=

# OpenClaw scoped device token (from `openclaw devices rotate` — carries operator scopes)
OPENCLAW_DEVICE_TOKEN=

# Optional: override CLI paths if not on system PATH
# CLAUDE_BIN=claude
# CODEX_BIN=codex
